# Semantic Model Classifier

Route to a semantic-model workflow.

Strong evidence:

- Explicit `Ticket Type: Semantic Model`
- Changes to relationships, tables, columns, cultures, expressions, or model metadata
- Paths under `*.SemanticModel/definition/`

Guardrails:

- Do not redefine governed business logic without explicit authorization.
- Validate affected reports and queries where possible.

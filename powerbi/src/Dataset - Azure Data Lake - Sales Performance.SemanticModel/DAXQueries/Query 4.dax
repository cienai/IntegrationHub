DEFINE
    -- 1. UDF to create the Virtual Table
    FUNCTION fnGenerateRepTable = (won_amt: expr) => 
        SUMMARIZECOLUMNS(
			users[trueai_full_name],
			
			"Measure", won_amt
			
        )

/* After defining the table logic, we use it to calculate the Stats */
EVALUATE
fnGenerateRepTable( [meas.stkhlds_all_act_email_count])
